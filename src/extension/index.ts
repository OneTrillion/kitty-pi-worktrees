import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

// Phase 1 entry point only. Commands and session-scoped resources follow later.
export default function worktreeExtension(_pi: ExtensionAPI): void {}
